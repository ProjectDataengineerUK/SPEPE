# Minimal dependencies for CI test suite — avoids heavy ML packages
# that have numpy 2.x compatibility issues (umap-learn, geopandas, pymc, shap)

# Core data
numpy>=1.26
pandas>=2.0
pyarrow>=15.0

# ML — only what tests actually call
scikit-learn>=1.3

# Config / validation
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
pyyaml>=6.0
tenacity>=8.0

# HTTP
requests>=2.31
httpx>=0.25
aiohttp>=3.9

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# Type stubs (needed for mypy in lint job)
types-requests>=2.31.0
types-pyyaml>=6.0.0
